extends ./layout

block script
  script.
    window.user = !{JSON.stringify(user)}
    window.config = !{JSON.stringify(config)}

block body
  body
    .site-wrapper
      .site-wrapper-inner#content
